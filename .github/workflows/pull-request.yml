name: Hello world action

on:
    - pull_request

jobs:
    hello_world_job:
        name: Hello world job
        runs-on: ubuntu-latest
        steps:
            - name: Print message to terminal
              run: echo "Hello!"